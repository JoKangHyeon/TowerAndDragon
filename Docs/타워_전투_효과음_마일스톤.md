# 타워 전투 효과음 마일스톤

작성일: 2026-08-27
설계 원본: [타워 전투 효과음 설계](./타워_전투_효과음_설계.md) — ID·클립·믹싱 수치는 전부 그쪽이 기준이다.
이 문서는 **순서·작업 항목·완료 조건·진행 상태**만 담는다. 표를 복제하지 않는다.

## 진행 현황

| M | 내용 | 상태 |
|---|---|---|
| M0 | 데이터 구조와 `SoundId` 추가 | ✅ 완료 (커밋 ed41ffa9) |
| M1 | 위치 기반 재생 오버로드 + 투사체 Launch/Resolve 연결 | ✅ 코드 완료 · 마나 타워로 실제 청취 확인 |
| M2 | 오라 `AuraOn` 런타임 연결 | 🟨 코드 완료 · 프리팹 4개 배선 대기 |
| M3 | `SoundCatalog.asset` 항목 17개 추가 | ⬜ 대기 |
| M4 | 타워·새끼용 데이터 배선, 씬 SE 풀 | ⬜ 대기 |
| M5 | 단일 타워 기능 검증 | ⬜ 대기 |
| M6 | 실전 웨이브 믹싱·마감 | ⬜ 대기 |

## 공통 규칙

- 한 번에 마일스톤 하나만 완료 처리하고, 끝나면 위 표와 해당 절을 갱신한다.
- `.asset` / `.prefab` / `.unity`는 텍스트 편집기로 직접 고치지 않는다. Unity가 열려 있으면 재직렬화로 덮어쓰고, GUID 사고 이력도 있다. 에디터 스크립트 또는 Coplay MCP로만 배선한다.
- 코드 본문에 볼륨·피치·간격 리터럴을 넣지 않는다. 전부 `SoundCatalog.asset`에 둔다.
- 생성형 SFX 기능은 사용하지 않는다.
- 컴파일 확인은 Editor.log 최신 블록 + `Assembly-CSharp.dll` 갱신 시각으로 판정한다.
- 씬·프리팹 작업과 플레이 테스트는 먼저 사용자에게 직접 할지 물어본다.

---

## M0 — 데이터 구조와 `SoundId`

이미 되어 있는 것(작업본, 커밋 전):

- `Assets/Scripts/Sound/OptionalSoundId.cs` 신규 생성 완료
- `TowerData`에 `_launchSound` / `_resolveSound` / `_auraOnSound` + 프로퍼티 3개 추가 완료

남은 작업:

- 완료: `Assets/Scripts/Sound/SoundId.cs` 끝에 14~30 추가 (`TowerManaLaunch` 14 ~ `TowerAuraOn` 30)
- 완료: 컴파일 에러 0 확인 후 커밋

완료 조건:

- 기존 0~13(`UiButtonClick`~`Victory`) 값과 순서가 그대로
- 미배선 데이터의 기본값이 무음(`_isEnabled = false`)
- 컴파일 에러 0

커밋: `타워 전투 효과음 데이터 구조 추가`

---

## M1 — 투사체 Launch/Resolve 런타임 연결

- `SoundManager`에 `Play(SoundId, Vector3)` 오버로드 추가(화면 기준 감쇠 + 줌 감쇠 + `panStereo` 정위), `SoundCatalog`에 파라미터 5개 추가 (설계 §6-0)
- `TowerAttack.Fire()`의 `SoundManager.Play(SoundId.TowerFire)` 제거 → `_towerData.LaunchSound` + 발사 위치
- `TowerAttack`의 `projectile.Launch(...)` 호출에 `_towerData.ResolveSound` 인자 추가
- `Projectile.Launch()`에 `SoundId? resolveSound = null` 파라미터 추가, 검증 통과 후 필드에 저장
- `Projectile.ResetState()`에서 해당 필드를 `null`로 초기화
- 도착 처리 순서: `ApplyHit()` → `ProjectileVisual.OnHit()` → Resolve음 → `Release()`
- `TowerAllyHealer.Fire()`의 `TowerFire` 하드코딩과 `Launch()` 호출도 동일하게 교체

완료 조건:

- M3·M4 이전이라 아직 소리는 나지 않는다. 재생 호출이 도달하는지로 판정한다.
- 공격 타워·생명 타워 모두 Launch/Resolve 재생 호출이 각각 1회 도달(임시 로그 또는 브레이크포인트)
- 몬스터 투사체(`MonsterAttack` 경로)는 기본 인자 `null`로 무음 유지
- 기존 `Play(SoundId)` 호출부(UI·건설·몬스터·BGM)를 하나도 건드리지 않음
- 2D 재생 경로가 `panStereo`를 0으로 되돌려, 타워음 뒤에 난 UI음이 한쪽으로 치우치지 않음
- `Camera.main`이 없는 씬에서 예외 없이 동작
- 풀에서 재사용된 투사체가 이전 Resolve음을 기억하지 않음
- 실제 청취 확인은 M5에서 한다
- 이 커밋 시점에는 모든 타워·새끼용이 무음이다(TowerFire 제거 + 카탈로그·배선은 M3·M4). 회귀가 아니다
- 컴파일 에러 0

커밋: `타워 투사체 발사 및 명중 효과음 연동`

---

## M2 — 오라 `AuraOn` 런타임 연결

- 완료: `Assets/Scripts/Sound/TowerAuraSoundEmitter.cs` 신규 작성 (설계 §6-4)
  - `LateUpdate()`에서 `TowerAuraSystem.TryGetActiveAura()`로 활성 여부 관측
  - 유효 반경이 직전 관측보다 커졌을 때 타워 위치와 함께 재생 (인구 추가로 범위가 넓어질 때마다)
- 프리팹 4개에 컴포넌트 추가: `TP_TimeTower` / `TP_StealthTower` / `TP_SoulTower` / `TP_EnhancementTower`

완료 조건:

- M3·M4 이전이라 아직 소리는 나지 않는다. 전환 감지가 맞는지로 판정한다.
- 인구 0·마비·부활 중에는 재생 호출이 없음
- 인구를 넣어 범위가 커질 때마다 재생 호출이 1회씩
- 범위가 줄거나 그대로일 때는 호출 없음
- 프레임마다 반복 호출 없음
- 실제 청취 확인은 M5에서 한다

커밋: `오라 타워 활성 효과음 연동`

---

## M3 — `SoundCatalog.asset` 항목 추가

- 기존 항목 뒤에 `_id` 14~30을 순서대로 append
- 클립은 설계 §7-1 표, 초기 수치는 설계 §8 표를 그대로 입력
- 재생 파라미터 5개 입력: `_seFullVolumeViewportRatio` 1.0 / `_seCutoffViewportRatio` 1.8 / `_sePanAmount` 0.6 / `_seZoomReferenceSize` 6 / `_seMinZoomVolumeScale` 0.4
- 마나·대공·생명 Launch가 같은 클립을 참조하는지 확인(항목은 별개)

완료 조건:

- 기존 0~13 항목의 `_id`와 클립 GUID가 변하지 않음
- 신규 ID 17개가 각각 정확히 1회 존재
- `Assets/Imported`에 변경 없음

---

## M4 — 타워·새끼용 데이터 배선과 씬 SE 풀

- `TD_*` 12종을 설계 §11 배선표대로 채운다 (가시·`TD_LifeTower_old`는 제외)
- `BD_*` 5종의 `_launchSound`에 기존 `TowerFire` 배선 (회귀 방지, Resolve/AuraOn 없음)
- `SampleScene.unity` / `Tutorial.unity`의 `SoundManager._sePoolSize`를 24로 변경

주의:

- `OptionalSoundId` 키는 아직 어떤 `.asset`에도 직렬화되어 있지 않다. Unity가 다시 저장해야 `_launchSound: {_isEnabled, _id}`가 생긴다. 반드시 에디터 경로로 배선한다.
- `TowerManaLaunch`의 대상 파일명은 `TD_Arrow`다. 배선 실수가 아니다.

완료 조건:

- 인스펙터에서 12개 타워 데이터를 배선표와 한 줄씩 대조 완료
- 새끼용 5종이 기존과 같은 소리를 냄

커밋: `타워별 효과음 에셋 배선 및 믹싱 적용`

---

## M5 — 단일 타워 기능 검증

- 설계 §10-1, §10-2 체크리스트를 플레이 모드에서 타워 한 종류씩 확인

완료 조건:

- 두 체크리스트 전부 통과
- 콘솔에 신규 NullReference·누락 참조 없음

---

## M6 — 실전 웨이브 믹싱·마감

- 타워 10대 이상으로 밤 웨이브 완주, 1배속·2배속 비교
- 조정 순서: ① 화면 감쇠 비율 2개 → ② 줌 감쇠 2개 → ③ 풀 크기 → ④ MinInterval → ⑤ Volume → ⑥ PitchRange → ⑦ `_sePanAmount`
- 줌아웃 시 볼륨이 너무 죽어 전장 파악이 어려우면 `_seMinZoomVolumeScale`을 0.6~0.7로 올린다
- 정위가 과하면 `_sePanAmount`를 낮춘다. 0이면 완전히 꺼진다(코드 되돌리지 않음)
- 카메라를 전장 안팎으로 옮기며 컷오프 경계가 자연스러운지 확인
- 풀 24개가 실제로 포화되는지 확인. 포화되면 우선순위 규칙을 별도 설계로 올린다
- `MinInterval`은 `Time.unscaledTime` 기준이라 2배속에서 더 많이 걸러진다. 두 배속을 오가며 타협점을 잡는다
- 판정은 플레이 중이 아니라 녹화본을 다시 들으며 한다
- 설계 §10-3 회귀·믹싱 체크리스트 확인
- 마나·대공·생명이 같은 클립인데 구분되는지 청취
- 설계 §7-3의 머스킷·얼음 Launch/Resolve 순서 최종 판단
- 조정한 볼륨·피치·MinInterval을 설계 §8에 기록

완료 조건:

- 소리가 자주 끊기지 않고 동시 발사에서 볼륨이 폭증하지 않음
- BGM·몬스터 핵심음·UI음을 과도하게 가리지 않음
- SE 볼륨 0에서 신규 소리가 전부 사라짐

커밋: `타워 전투 효과음 플레이 검증 및 수치 조정`
