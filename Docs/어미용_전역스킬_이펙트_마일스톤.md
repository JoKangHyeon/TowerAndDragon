# 어미용 스킬 이펙트 마일스톤 (2026-09-01 ~)

작성일: 2026-09-01
설계 원본: [어미용 스킬 이펙트 설계](./어미용_전역스킬_이펙트_설계.md) — **수치·자산 경로·표는 전부 그쪽이 기준이다.**
이 문서는 **순서·작업 항목·완료 조건·진행 상태**만 담는다. 설계 문서의 표를 복제하지 않는다.

---

## 이 문서를 처음 여는 에이전트에게

1. 브랜치는 `feature/motherDragon-effect`. 이 작업은 **문서 2개만 있고 코드·자산은 아직 0**인 상태에서 시작했다.
2. **「진행 현황」 표를 믿지 말고 아래 「현재 상태 확인 명령」을 먼저 돌린다.**
   표는 사람이 손으로 갱신하므로 밀릴 수 있고, 명령은 저장소의 실제 상태를 본다.
3. 한 번에 마일스톤 **하나만** 완료 처리한다. 끝나면 「진행 현황」 표와 해당 M 절의 「작업 로그」를 갱신한다.
4. **작업 전에 「뒤집힌 결정」 절을 반드시 읽는다.** 옛 판의 문장("성 앵커", "타워수리", "4종")을 보고
   되돌리지 말 것. 근거는 설계 문서 §0의 정정 표다.
5. 씬·프리팹 작업과 플레이 테스트는 **먼저 사용자에게 직접 할지 물어본다.**
6. 이 작업의 전체 그림은 딱 두 계층이다 — **스킬 발동 시 뜨는 화면 배경 이펙트(A)** 와
   **각 스킬의 대상에 뜨는 이펙트(B)**. 그 이상을 늘리지 않는다.

---

## 작업 원칙

- `.asset` / `.prefab` / `.unity`를 텍스트 편집기로 직접 고치지 않는다. Unity가 열려 있으면 재직렬화로
  덮어쓰고, GUID 사고 이력도 있다. 에디터 스크립트 또는 Coplay MCP로만 배선한다.
- Coplay의 **생성형 기능은 사용 금지**(3D·텍스처·이미지·음악·SFX·TTS 생성, 오토 리깅, 애니메이션
  라이브러리 검색). 자산이 필요하면 기존 팩 조합 또는 팀 요청.
- **`Assets/Imported`는 수정 금지 원본**이다. 팩 프리팹을 직접 고치지 말고 **파생본**을 만든다.
  완성 프리팹은 `Assets/Imported`(별도 저장소)에 커밋한다.
- 비동기는 **UniTask**. `StartCoroutine`/`IEnumerator` 금지, `async void` 금지,
  fire-and-forget은 `.Forget()`, 취소 토큰은 `this.GetCancellationTokenOnDestroy()`.
- 이벤트 구독은 `OnEnable`/`Awake`, 첫 발화는 `Start` 이후.
- 코드 본문에 문자열·리터럴 상수를 넣지 않는다(`_LOC_KEY`, `Defines`, 상수는 UPPER_SNAKE_CASE).
- **이펙트는 자식 이름으로 판단하지 말고 렌더해서 확인한다.** 이름과 실제 그림이 어긋난 전례가 있다.
- **커밋은 사용자가 직접 한다.** 각 M의 커밋 메시지만 이 문서에 적어 둔다.
- 검증은 Test Runner가 아니라 `OnValidate` + 플레이 확인으로 한다.
- 이펙트 생성기 스크립트(`CoplayScripts/`)는 작업이 끝나면 삭제 예정인 **임시 도구**다. 결과물은 프리팹이다.

---

## 진행 현황

| M | 내용 | 속성 | 신규 자산 | 상태 |
|---|---|---|---|---|
| **M0** | 설계 문서 1차 개정 | — | 0 | ✅ 완료 (2026-09-01) |
| **M1** | 빙결 N-동시발동 실측 → N-cap `[미정]` 해소 | 얼음 | 0 | ✅ 완료 (2026-09-01) — **N-cap 불필요** |
| **M2** | A 오버레이 기반 (호스트 + 빙결 A 파일럿) | 얼음 | 1 | ⬜ 미착수 |
| **M3** | 발동 결과 전달 (`Activate` bool + 대상 0 판정) | — | 0 | ✅ 코드 완료 (2026-09-01) · 플레이 확인 남음 |
| **M4** | 성벽 재생 A·B | 생명 | **0** | ✅ 코드 완료 (2026-09-01) · 에셋 배선·플레이 확인 남음 |
| **M5** | 전역 화염 A·B | 불 | 2 | ⬜ 미착수 |
| **M6** | 즉시 재활성화 A·B | 시간 | 2 | ⬜ 미착수 |
| **M7** | 메테오 방벽 A·B + 방벽 생애주기 | 암석 | 3 | ⬜ 미착수 |
| **M8** | 마감 — 5종 연속 발동 회귀·성능·정리 | — | 0 | ⬜ 미착수 |

**전역 빙결에 별도 M이 없는 이유**: B가 이미 배선돼 있어(`FX_Status_IceFreeze`) **M1 + M2로 완결**된다.

### 왜 이 순서인가

- **M1이 맨 앞** — 신규 자산 0개로 **오늘 당장 가능**하고, 그 결과 숫자가 M5·M7의 경량화 목표치를 정한다.
- **M2가 M3보다 먼저** — 설계 §3-2의 변환 3개(무한 루프 → 페이드, 파티클 1000×3 경량화, 정렬 미설정)가
  이 작업 **최대의 미지수**다. 파일럿에서 깨지면 A계층 형태 자체를 다시 정해야 하므로,
  배선을 다듬기 전에 먼저 깨 본다.
- **M4가 M5보다 먼저** — B 대상이 성 **1개**라 5종 중 가장 싸고, A와 B가 화면상 같은 위치에 겹치는
  유일한 케이스라 두 계층이 서로를 가리지 않는지 최소 비용으로 확인된다.
- **M4~M7은 서로 독립**이다. M3만 끝나 있으면 순서를 바꿔도 된다.

---

## 현재 상태 확인 명령

진행 현황 표가 밀려도 지금 어느 M에 있는지 알 수 있다. **저장소 루트에서 실행한다.**
괄호 안이 "아직 미착수일 때의 정상 결과"다.

```bash
cd /Users/ihaneul/unityProject/TowerAndDragon

# M0: 설계 문서가 개정됐나 (3줄 나오면 완료. 결과 없으면 아직 초판이다)
grep -n "1차 개정" Docs/어미용_전역스킬_이펙트_설계.md

# M2: A 오버레이 호스트 스크립트가 있나 (미착수면 결과 없음)
ls Assets/Scripts/UI/Skill/ | grep -i overlay

# M2/M4~M7: 오버레이 프리팹이 몇 속성까지 됐나 (최종 5개. 미착수면 디렉터리 없음)
ls Assets/Imported/Prefabs/SkillCastOverlay/ 2>/dev/null | grep -c prefab$

# M3: Activate가 bool로 바뀌었나 ("public void Activate"면 아직)
grep -n "Activate(in SkillCastContext" Assets/Scripts/UI/Skill/Skill.cs

# M3: 컨트롤러가 반환값을 받나 (대입이 없으면 아직)
grep -n "skill.Activate" Assets/Scripts/UI/Skill/SkillTargetingController.cs

# M5 안전선: DS_FireBurn을 실수로 채우지 않았나 ({fileID: 0}이어야 정상 - 설계 §2-2)
grep -n "_activeVfxPrefab" Assets/Data/Dragon/Status/DS_FireBurn.asset

# 빙결 B 안전선: 3개 다 채워져 있어야 정상 (지우면 빙결 연출이 통째로 사라진다)
grep -c "guid: 78f21a39a8c124524a6881ec5cecd6a1" Assets/Data/Dragon/Status/DS_IceFreeze*.asset

# M6: 재활성화 원인 분기에 연출이 붙었나 (미착수면 enum 선언과 호출부 2줄만 나온다)
grep -n "SkillRepair" Assets/Scripts/Buildings/Tower/Tower.cs

# M7: 방벽에 VFX 훅이 생겼나 (미착수면 결과 0)
grep -ciE "vfx|effect" Assets/Scripts/Buildings/StoneBarricade.cs
```

---

## 작업 범위 전체 지도

5스킬 × 2계층 = 10칸. 지금 채워진 건 1칸이다.

| 스킬 (속성) | A. 화면 오버레이 | B. 대상별 | 담당 M |
|---|---|---|---|
| 전역 빙결 (얼음) | ⬜ | ✅ **이미 있음** — `FX_Status_IceFreeze` | M1·M2 |
| 성벽 재생 (생명) | ⬜ | ⬜ | M4 |
| 전역 화염 (불) | ⬜ | ⬜ | M5 |
| 즉시 재활성화 (시간) | ⬜ | ⬜ | M6 |
| 메테오 방벽 (암석) | ⬜ | ⬜ (몬스터 + 방벽 3사건) | M7 |

---

## M0 — 설계 문서 1차 개정 ✅

`Docs/어미용_전역스킬_이펙트_설계.md`를 코드·에셋·스트링테이블과 대조해 개정했다.

바꾼 것:

- 범위를 4종 → **5종**으로 (성벽 재생 §2-5 신설)
- 구 §3 "성 앵커 월드 이펙트" 폐기 → **화면 전역 배경 오버레이**로 재작성 (§3)
- "타워수리" → **"즉시 재활성화"**, 연출 의미를 "기다림을 건너뛴다"로 재정의 (§2-3)
- §1에 계층별 스폰 방법 표 추가 (`PlayForSeconds`가 `void`라는 제약 반영)
- §2-4에 방벽 생애주기 3사건 표 추가 (피격·소멸이 초판에 없었다)
- §5에 구멍 2개 추가 (대상 0 → `true` 문제 / `SkillUsed` 튜토리얼 의존)
- §6-1 색조를 3색 → **5속성 표**로 (`DragonAttributePalette.cs` 기준)
- §7을 막연한 "신규 조합"에서 **실존 확인된 프리팹 경로 표**로 교체 + 이름 함정 3개
- §9 마일스톤 목록을 지우고 이 문서 포인터로 대체

완료 조건 (전부 충족):

- §0 표가 5행 + 정정 표 3행
- 헤더에 `1차 개정: 2026-09-01`
- H1/도입부가 "액티브 5종(전역 4 + 단일 대상 1)"
- `[미정]` 표기가 §4·§8에 유지
- 두 문서가 같은 표를 두 벌 갖지 않음

작업 로그:

- 2026-09-01 — 개정 완료. 219줄 → 449줄.

---

## M1 — 빙결 N-동시발동 실측

**신규 자산 0개. 코드 0줄. 오늘 당장 가능하다.** 설계 §4의 `[미정]`을 숫자로 바꾸는 것이 목적이다.

선행: 없음.

작업:

- 밤 웨이브에서 몬스터가 **20마리 이상**일 때 전역 빙결을 눌러 관찰한다
- 관측 항목
  - 화면에 동시에 뜬 `FX_Status_IceFreeze` 개수
  - 겹쳐서 개별 몬스터 판독이 되는지 (개체를 구분할 수 없으면 그게 상한이다)
  - 프레임 하락 정도
  - `Projectile` 레이어에 뎁스 정렬이 없어 앞뒤가 뒤섞이는 정도 (설계 §4)
- 가능하면 녹화해서 다시 본다. 플레이 중 판정은 부정확하다

⚠️ **한 프레임 스냅샷이 아니라 누적 중첩을 본다.** 빙결 B는 3/4/5초 지속이라
(`DS_IceFreeze` 3 / `_R1` 4 / `_R2` 5) 발동 직후보다 몇 초 뒤가 더 붐빈다.

⚠️ 이 실측은 **B계층의 상한**을 재는 것이다. A 오버레이는 항상 1개라 여기에 안 걸린다.

완료 판정 (전부 충족):

- ✅ 설계 문서 §4의 `[미정]`을 결론으로 교체했다 (§4-1 신설)
- ✅ 경량화 필요 여부 판단이 섰다 — **불필요**
- ✅ 컷 정책이 정해졌다 — **자르지 않고 다 띄운다**

### 결론: N-cap을 두지 않는다

**성능·판독성 둘 다 상한에 닿지 않았다.** 상세 근거·표는 설계 §4-1에 있다(여기 복제하지 않는다).
요지만: 빙결(56 파티클/마리)은 이미 게임에서 다수에게 동시에 도는 화상·둔화(각 332)보다 **6배 가볍고**,
화면에 20마리가 차도 큐브가 반투명이라 개체가 읽힌다.

**컷 정책을 `MonsterStatusVfx.Refresh`에 넣으려 하지 말 것.** 거기는 화상·둔화를 포함한
**모든 상태이상 VFX의 공유 경로**라, 빙결 캡이 아니라 전역 캡이 된다.

### M1이 부수적으로 찾아낸 것 3개 (M5·M7이 물려받는다)

1. **연출의 주역은 파티클이 아니라 스프라이트 1장이다** — `IceShell`(SpriteRenderer)을
   `StatusVfxBodyFit`이 몬스터 몸통 크기에 맞춰 늘린다. Tank급은 화면 폭의 ~17%, 소형은 ~5%.
   **화면을 덮는 양은 마릿수가 아니라 몬스터 구성이 정한다.**
2. **몬스터 연출이 안개 틴트를 안 따라간다** — 설계 §6에 적었다. **B계층 작업 전에 읽을 것.**
3. **`IceShell`이 뎁스 정렬에 참여하지 않는다** — 설계 §6. 성 뒤 몬스터의 큐브가 성탑 위로 올라왔다.

### 방법 — 실제로 어떻게 쟀나 (다시 재야 할 때를 위해)

Coplay에는 **게임에 입력을 넣는 도구가 없다.** 그래서 손으로 12일차까지 플레이하는 대신,
실제 웨이브·실제 몬스터·실제 카메라를 그대로 쓰고 진행 절차만 스크립트로 건너뛰었다.

- 도구: `CoplayScripts/MeasureFreezeDensity.cs` (1회성. 진단용 `DumpMonsterVisibility.cs`,
  `DumpCameraAndMonsters.cs`, `CheckSceneDirty.cs`도 같이 만들었다)
- 씬: `Haneul_TowerEffectScene` (`SampleScene`이 아니다 — 그쪽에 저장 안 된 작업이 있었다)
- 웨이브: `CodexWave_13`(38마리). 포탈을 `SetSpawnActive(true)`로 강제 개방.
  ⚠️ **살아 있는 스케줄은 `WaveCycleSchedule_Codex`다.** `Assets/Data/WaveData/Main Waves/`는
  어느 씬에서도 참조되지 않는 낡은 데이터이므로 그쪽 숫자로 계획을 세우지 말 것
- 빙결은 스킬 해금을 건너뛰고 `DS_IceFreeze`를 직접 걸었다 —
  `FreezeAllSkill.ApplyEffect`가 하는 일이 `monster.ApplyStatus` 하나뿐이라 결과가 같다
- **화면 안 마릿수**가 임계를 넘을 때마다 `timeScale=0`으로 세우고 연출 켠/끈 화면을 한 쌍씩 촬영
- 결과: `%TEMP%/freeze_density/` (log.txt + N38_*inview_{on,off}.png 14장).
  가장 결정적인 쌍은 `N38_20inview_on.png` / `_off.png`

### 이 측정에서 겪은 함정 4개 (같은 걸 또 밟지 말 것)

1. **⚠️ 몬스터를 포탈에서 얼리면 안 된다.** 미탐색 구역은 안개가 덮어 몬스터가 보이지 않는다.
   처음에 그렇게 재서 **안개 타일만 찍힌 사진**을 얻었다. 재는 것은 필드 전체 마릿수가 아니라
   **화면에 보이는 마릿수**다.
2. **⚠️ URP라 새 카메라 + `RenderTexture`로는 못 찍는다.** `Camera.CopyFrom`이 URP 2D 렌더러 설정을
   옮기지 못해 스프라이트가 하나도 안 담겼다. 메인 카메라를 그대로 쓰고 `ScreenCapture`로 찍는다
   (게임 뷰가 361x203로 작아 `superSize` 4배 필요).
3. **⚠️ 연출을 끌 때 `ParticleSystemRenderer`만 끄면 안 된다.** 주역이 `IceShell`(SpriteRenderer)이라
   `Renderer` 전체를 봐야 한다. 이걸 놓쳐서 on/off 사진이 똑같이 나왔다.
4. **⚠️ Coplay는 호출마다 새 어셈블리로 컴파일해 `static` 상태가 호출 간에 유지되지 않는다.**
   첫 호출에 등록한 `EditorApplication.update` 루프는 계속 돌지만 다음 호출에서 조회할 수 없다.
   **진행 상황과 결과는 전부 파일로 떨궈야 한다.**

작업 로그:

- 2026-09-01 — 실측 완료. N-cap 불필요로 확정. 설계 §4-1 신설, §6에 안개 틴트·뎁스 정렬 항목 추가,
  §8의 `[미정]` 항목 해소.

---

## M2 — A 오버레이 기반 · 빙결 A 파일럿

**이 작업 최대의 미지수를 여기서 깬다.** 나머지 4속성이 같은 틀을 탈 수 있는지 확인하는 게 목적이다.

선행: M1 (경량화 목표치를 알고 시작하는 편이 낫다. 급하면 병행 가능)

### 진행 상태 (2026-09-01)

- ✅ **자산 5종 전부 완료** — Codex가 `Assets/Imported/Prefabs/SkillCastOverlay/`에 만들었다
  (계획은 얼음 1개 파일럿이었으나 5속성이 한 번에 나왔다)
- ✅ **설계 §3-2의 변환 3개 처리 완료** — 아래 「자산 검수 결과」
- ✅ **호스트 코드 작성 완료** — `Assets/Scripts/UI/Skill/SkillCastOverlayHost.cs` (컴파일 에러 0)
- ✅ **씬 배치·배선 완료** — `Haneul_TowerEffectScene`의 `Main Camera/SkillCastOverlayHost`
  (`CoplayScripts/WireSkillCastOverlayHost.cs`로 배선. **씬은 저장하지 않았다** — 사람이 확인 후 저장)
- ✅ **생애주기 확인 완료** — 아래 「호스트 동작 실측」
- ⬜ **불 강도 판단** — 남음 (아래)
- ⬜ **다른 씬(SampleScene·Tutorial) 배치** — 남음

### 호스트 동작 실측 (`CoplayScripts/TestSkillCastOverlayHost.cs`, 얼음)

| 경과 | 살아 있는 입자 | 오브젝트 |
|---|---|---|
| 0.4초 | 260 | 켜짐 |
| 1.2초 | 199 | 켜짐 |
| 2.0초 | 142 | 켜짐 |
| 3.5초 | 97 | 켜짐 (화면상으로는 거의 안 보임) |
| 6.0초 | 0 | **꺼짐** |

- **뚝 끊기지 않고 점진적으로 준다** — `StopEmitting` 방식이 의도대로 동작한다
- 켜진 채 남지 않는다
- 입자는 6초까지 남지만 **눈에 보이는 시간은 약 2초**다. 얼음 `Glow`의 수명이 5초라 꼬리가 길 뿐
  화면에는 영향이 없다 — 수명을 줄일 이유가 없다
- 콘솔에 이 기능 관련 신규 에러·경고 없음

### ⚠️ 남은 판단 — 불 강도

효과가 **화면에 보이는 시간이 약 2초**로 확인됐다. 불은 그 2초 동안 화면 전체가 불길이라
성·유닛이 잘 안 보인다. 밤 전투 중에 쓰는 스킬이므로 줄이는 쪽이 맞아 보이지만 취향 판단이다.
줄인다면 레버는 `FireUpside`·`FireLeftSide`·`FireFast`의 `rateOverTime`(현재 20/20/16)이다.
**프레이밍은 되돌리지 말 것** — 방출기가 화면 밖에 있던 건 명백한 결함이었다.

### 자산 검수 결과 (렌더해서 확인)

Codex가 처음부터 맞춘 것: `maxNumParticles` 1000 → **128** (실측 동시 파티클 250~280개/오버레이),
`scalingMode: Local`(설계가 경고한 "카메라 자식이면 부모 스케일이 곱해진다" 함정 회피).

고친 것 2개 (`CoplayScripts/FixSkillCastOverlaySorting.cs`, `FixFireOverlayFraming.cs`):

1. **소팅 `Overlay:100` → `Highlight:-100`** — ⚠️ 이 프로젝트 레이어 순서는
   `Default → Building → Overlay → Projectile → Fog → Highlight`이고, `FogCloudRenderer`가 미탐색
   셀에 까는 구름 타일맵이 **Fog**다. Overlay에 두면 **구름에 덮여** "화면 전역"이 성립하지 않는다.
   음수 order인 이유: Highlight는 `MouseSelect`(order 0)·카드류(order 1)가 이미 쓰는 마커 레이어라
   덮으면 안 된다. -100이면 **구름 위·마커 아래**다.
   (지형 안개는 무관하다 — `FogOfWarRenderer`는 별도 지오메트리 없이 지형 타일 **색을 직접 틴트**한다.)
2. **불 프레이밍** — 카메라 ortho 5 · 종횡비 1.78이라 화면은 가로 17.8 × 세로 10 단위인데,
   불만 방출기가 화면 밖으로 밀려 있었다(`Sparks`는 pos `(-12,-7.4)`로 **전체가 밖**). 다른 속성의
   관례(Box 폭 22)에 맞춰 중앙 정렬 + 확장. **방출률은 건드리지 않았다** — 같은 입자가 넓게 퍼지게만 했다.

덮개(기준 화면과 채널차 12 이상인 픽셀 비율) 변화:

| 속성 | 수정 전 | 수정 후 |
|---|---|---|
| 얼음 | 75.4% / 평균차 44.9 | 73.7% / 45.5 |
| 불 | 31.2% / 17.7 | **46.0% / 35.7** |
| 생명 | 45.0% / 14.7 | 43.2% / 17.7 |
| 암석 | 58.7% / 31.0 | 59.4% / 33.8 |
| 시간 | 52.9% / 22.8 | 48.9% / 19.4 |

⚠️ **남은 판단: 불이 과할 수 있다.** 프레이밍을 고치자 화면 전체가 불길이 돼 성·유닛이 잘 안 보인다.
**호스트의 지속시간에 달린 문제**라(0.3초 번쩍이면 괜찮고 3초 깔리면 문제) 플레이 확인 후 정한다.
줄인다면 `FireUpside`·`FireLeftSide`·`FireFast`의 `rateOverTime`이 레버다.

⚠️ **`looping: 1`은 고치지 않는다.** 설계 §3-2가 변환 대상으로 적었지만, 호스트가
`StopEmitting`으로 방출만 멈추고 남은 입자가 수명대로 사라지게 하므로 루프인 채로 맞다.
Codex가 그대로 둔 것이 옳다.

작업:

- ~~**자산 1개** — Hovl `Snow.prefab`(설계 §3-2 얼음 1순위)에서 오버레이용 파생 프리팹을 만든다.~~
  설계 §3-2의 변환 3개를 전부 적용한다:
  1. `looping: 1` → 페이드 인/아웃으로 끝나게 (호스트가 제어)
  2. `maxNumParticles: 1000` × ParticleSystem 3개 → 경량화
  3. `m_SortingLayer: 0`(Default) → 화면 최상단 레이어로 재지정
- **코드 1개** — 신규 호스트 컴포넌트. 설계 §3-3의 `TowerBuffOrbCoordinator` 자가등록 패턴
  (`:33` 정적 필드 / `:36` `[RuntimeInitializeOnLoadMethod(SubsystemRegistration)]` /
  `:49` `OnEnable` 대입 / `:56` `OnDisable` 해제). 페이드는 UniTask
- **씬** — 오버레이를 카메라 자식으로 배치

### M2에서 결정할 것 2개 (결과를 이 칸에 적는다)

**① 어느 속성 오버레이를 띄울지 아는 방법** — 결정: **권고안 채택 (`DragonTreeManager.ActiveAttribute`)**

- 권고: 호스트가 `DragonTreeManager` 참조를 갖고 `ActiveAttribute`를 읽는다.
  `SkillHudBinder.cs:104`가 증명하듯 HUD는 `SkillManager.AvailableSkills`(해금 + **활성 속성 일치**)만
  노출하므로, 발동 가능한 액티브는 항상 활성 속성의 것이다
- 대안: `SkillData`에 `DragonType` 필드 추가 — 공용 데이터 클래스 + 에셋 5개를 건드린다

**② 호스트를 씬에 놓을지, 첫 사용 시 자동 생성할지** — 결정: **권고안 채택 (씬 배치, 카메라 자식)**

- 권고: **씬 배치**(카메라 자식) + `[SerializeField]`로 5속성 프리팹 참조, `OnValidate`로 누락 검사
- `ProjectilePool`에 자동 생성 선례가 있으나(`타워_투사체_연출_마일스톤.md` M2), 오버레이는 프리팹
  5개를 참조해야 해서 직렬화 소스가 필요하다

### ⚠️ 함정

- ⚠️ **`scalingMode: 0`(Hierarchy)** — 카메라 자식으로 붙으면 카메라의 스케일이 파티클에 곱해진다.
  화면을 안 채우거나 거꾸로 폭발하면 여기부터 본다
- ⚠️ **`MinMaxCurve.constant`는 `constantMax`의 별칭이다.** 경량화하면서 `constant`·`constantMin`·
  `constantMax`를 다 곱하면 배수가 **제곱으로** 걸린다 (작업노트 3-10 ★)
- ⚠️ **`UI_CameraInputBlocker`를 붙이지 않는다.** 오버레이는 창이 아니다. 붙이면 밤 전투 중 카메라가 죽는다
- ⚠️ **이 시점의 배선은 임시다.** `SkillUsed`에 무조건 물려 그림만 검증한다.
  **M3을 건너뛰면 아무 일도 안 일어난 시전(고칠 타워 0개, 몬스터 0마리, 성 만피)에도 화면 전체에
  오버레이가 뜬 채로 남는다.** M2를 커밋하더라도 M3 전에는 완성이 아니다
- ⚠️ Hovl 프리팹 원본을 고치지 않는다. `Assets/Imported`는 수정 금지 원본이다 — 파생본을 만든다

완료 판정:

- 발동하면 화면 전체에 얼음 배경이 뜨고 **뚝 끊기지 않고 페이드로 사라진다**
- 게임 오브젝트·UI에 가려지지 않고 의도한 층에 그려진다
- 카메라를 움직여도 오버레이가 화면에 붙어 있다 (월드에 남지 않는다)
- 파티클 총량이 M1에서 정한 목표치 이하
- **나머지 4속성이 같은 틀을 탈 수 있다고 판단된다** — 이게 이 M의 진짜 산출물이다

커밋: `어미용 스킬 시전 화면 오버레이 기반 및 빙결 연출 추가`

작업 로그:

- (비어 있음)

---

## M3 — 발동 결과 전달

M2의 임시 배선을 정확한 조건으로 바꾼다. 설계 §5 전체가 이 M이다.

선행: M2

### 진행 상태 (2026-09-01) — 코드 완료, 컴파일 에러 0

바꾼 것 (전부 `Assets/Scripts/UI/Skill/`):

| 파일 | 변경 |
|---|---|
| `Skill.cs` | `Activate`를 `void` → **`bool`** (호출부는 `ActivateSkill` 한 곳뿐, grep으로 확인) |
| `Skill.cs` | `FreezeAllSkill` — 실제 얼린 마릿수를 세어 **0이면 `false`** |
| `Skill.cs` | `GlobalCurrentHealthDamageSkill` — 같은 방식 |
| `Skill.cs` | `HealCastleSkill` — **만피·사망이면 `false`** (`Health.Heal`이 조용히 아무 일도 안 하는 구간) |
| `SkillTargetingController.cs` | `ActivateSkill`이 반환값을 보고 오버레이 호출 |
| `SkillCastOverlayHost.cs` | M2 임시 `SkillUsed` 구독·`_skillTargetingController` 필드 제거 |

**`SkillUsed`는 조건 밖에 그대로 뒀다.** 두 신호의 의미가 다르기 때문이다 -
`SkillUsed`는 "**눌렀다**"(튜토리얼용, 효과 없어도 발화해야 함), 오버레이는 "**효과가 있었다**"다.
그래서 `SkillUsed.Invoke`는 무조건, 오버레이는 `if (hasTakenEffect)` 안에서 호출한다.

**호스트는 스킬 쪽 이벤트를 구독하지 않는다.** `Activate`의 반환값을 아는 건 `ActivateSkill`뿐이라
구독 방식으로는 조건을 걸 자리가 없다. 컨트롤러가 정적 진입점
`SkillCastOverlayHost.PlayForActiveAttribute()`를 직접 부른다 - 씬 배선이 늘지 않는 대신
컨트롤러가 호스트를 직접 참조하게 되는 것을 감수했다(대안: 컨트롤러에 `SkillTookEffect` 이벤트 신설).

⚠️ **호스트의 인스펙터 참조가 4개 → 3개로 줄었다** (`_dragonTreeManager`, `_cycleManager`,
`_gameManager`). 이미 배선한 씬이 있으면 다시 배선할 필요는 없지만, 인스펙터에 빈 칸이 남아 보일 수 있다.

### 설계 문서와 달랐던 점

- 설계 §5·이 문서 원안은 "`ApplyEffect`를 bool로 전환"처럼 읽히지만, **`ApplyEffect`는 이미 8개 전부
  bool을 정확히 반환하고 있었다.** 실제로 빠진 것은 `Activate`가 그 값을 **버린다**는 것뿐이었다.
- 설계 §5-1이 지적한 "대상 0마리여도 `true`"는 2곳이 아니라 **3곳**이다 —
  `AreaCurrentHealthDamageSkill`(`:350`)도 빈 땅을 찍어도 `true`를 돌려준다.
  **어미용 5종이 아니므로 이번 범위에서 제외했다**(범위 확인 결과). 별건으로 고칠 후보다.

작업:

- ~~`Skill.Activate`(`Skill.cs:256`)를 `bool` 반환으로 전환~~
- ~~대상 0마리 판정~~
- ~~`SkillTargetingController.ActivateSkill`(`:244`)에서 `true`일 때만 오버레이 호출~~
- ~~M2의 임시 `SkillUsed` 배선 제거~~

### ⚠️ 함정

- ⚠️ **`Skill.cs`는 모든 액티브가 공유하는 기반 코드다. 착수 전 사용자에게 확인받는다.**
- ⚠️ **`SkillUsed.Invoke`를 조건 안으로 옮기지 않는다.** `GuideQuestController.cs:152`·`:220`·`:358`이
  `TutorialConditionType.SkillUsed` 판정에 쓰고 6개 씬에 배선돼 있다. 안으로 옮기면 튜토리얼이 깨진다.
  **코드에 그 이유를 주석으로 남긴다** (설계 §5-1 ②)
- ⚠️ 반환값을 쓰는 호출부는 `SkillTargetingController` 한 곳뿐이다. 다른 곳에서 `Activate`를 부르는 코드가
  생겼는지 먼저 grep한다

완료 판정 (플레이 검증 완료 — `CoplayScripts/TestM3M4Behavior.cs`, `TestSkillUsedRegression.cs`):

- ✅ 컴파일 에러 0
- ✅ 몬스터 0마리에 전역 빙결 → `Activate=False`
- ✅ 성 만피에 성벽 재생 → `Activate=False`, 연출 0→0
- ✅ **튜토리얼 회귀 없음** — 성 만피(효과 없음, `Activate=False`)에서도 `SkillUsed` 발화 1회
- ⬜ 비활성 타워가 0개일 때 즉시 재활성화 (M6에서 함께 확인)
- ⬜ 대상이 0이면 쿨타임·자원도 소모되지 않는다 (코드상 `SpendResources`가 `ApplyEffect` 성공
  뒤에만 불리므로 성립. 눈으로는 아직 미확인)

커밋: `스킬 발동 결과 전달 및 시전 연출 조건부 재생`

작업 로그:

- 2026-09-01 — 코드 완료(`Skill.cs`, `SkillTargetingController.cs`, `SkillCastOverlayHost.cs`).
  컴파일 에러 0.
- 2026-09-01 — 플레이 검증 완료. 대상 0 판정 3건 + 튜토리얼 `SkillUsed` 회귀 없음 확인.
  콘솔에 이 기능 관련 신규 에러 없음(기존 `GuideAnchorRegistry` 경고만).

---

## M4 — 성벽 재생 A·B (생명)

**B 대상이 성 1개라 5종 중 가장 싸다.** A·B가 화면상 겹치는 유일한 케이스이기도 하다.

선행: M2, M3

### 진행 상태 (2026-09-01) — 코드 완료, 신규 자산 0개

**A는 M2에서 이미 끝났다** (`FX_DragonSkillCastOverlay_Life.prefab`). 이 M에서 한 것은 B다.

바꾼 것:

| 파일 | 변경 |
|---|---|
| `SkillSO.cs` | `TargetVfxPrefab` + `TargetVfxLifetimeSeconds` 필드 신설 (`[WiringOptional]`) |
| `Skill.cs` | `protected void PlayTargetVfx(Vector3)` 공용 헬퍼 추가 |
| `Skill.cs` | `HealCastleSkill.ApplyEffect`가 회복 성공 후 `PlayTargetVfx(castle.GroundWorldPosition)` |
| `DragonSkillTreeAssetGenerator.cs` | 생명 스킬에 `TargetVfxPrefab` 배선 추가 |
| `CoplayScripts/WireSkillTargetVfx.cs` | 기존 에셋에 필드만 채우는 1회성 도구 |

### ⚠️ 뒤집힌 결정 — B 자산을 새로 만들지 않는다

설계 §2-5는 `FX_BD_LifeHealWave`가 새끼용 타워 힐(`TA_BD_TowerHealEffect`)에 이미 배선돼 있어
**파생본을 만들라**고 했다. 실제 작업에서 **원본을 그대로 재사용하기로 뒤집었다.**

근거: 이 스킬은 **메인 성 1개에만** 발동한다. 새끼용은 타워에 뜨고 어미용은 성에 뜨며, 어미용에는
화면 전체 초록 오버레이(A)가 함께 붙으므로 화면에서 혼동되지 않는다. 파생본을 만들면 관리할
자산이 하나 늘고 두 연출의 톤이 갈릴 위험만 생긴다. → **M4의 신규 자산은 2개에서 0개가 됐다.**

### `PlayTargetVfx`는 M5·M7이 그대로 쓴다

`TowerHealAuraEffectSO`(`:46`)가 쓰는 관용구를 그대로 따랐다 —
`[WiringOptional] GameObject` + `ProjectilePool.PlayForSeconds(prefab, center, identity, lifetime)`.
스킬 코드마다 스폰 호출을 따로 쓰지 않도록 `Skill`에 올려 뒀다.

⚠️ **M6(즉시 재활성화)은 이 헬퍼로 안 된다.** 타워는 `Building` 레이어의 뎁스 정렬을 상속해야 하는데
(`tower.DepthSortOrder + 1`, `TowerBuffOrbDisplay.ApplySortingOrder` 참고) `PlayForSeconds`는
정렬을 넘길 수 없다. M6은 `Tower`가 호스트가 되어 직접 스폰하고 정렬을 매겨야 한다.

작업:

- ~~**A**: Hovl `Blurred flow green.prefab` 파생~~ → M2에서 완료
- ~~**B**: `FX_BD_LifeHealWave.prefab`의 **파생본**을 성 크기에 맞춰 제작~~
  → **원본 재사용으로 변경** (위 참고)
- ~~스폰 지점: `HealCastleSkill.ApplyEffect`~~

### ⚠️ 함정

- ⚠️ **`Castle.Repair`에 걸지 않는다** (설계 §2-5). `Castle.cs:172`는 세 경로가 공유한다 —
  ① 연구 `convenience_castle_regen_1`의 매일 낮 자동 회복(`CastleRegenSystem.cs:50`)
  ② 연구 `convenience_castle_repair`의 낮 즉시 수리 ③ 이 스킬.
  **여기에 걸면 매일 아침 연구 회복에도 뜬다.** `HealCastleSkill.ApplyEffect`에서 직접 스폰한다
- ⚠️ **`FX_BD_LifeHealWave` 원본을 그대로 쓰지 않는다.** 이미
  `Assets/Data/BabyDragon/Life/TA_BD_TowerHealEffect.asset`(새끼용 생명 타워 힐)에 배선돼 있어,
  재사용하면 두 연출이 같은 그림이 된다
- ⚠️ B는 성(`Building`)에 붙으므로 뎁스 정렬을 상속한다. `Overlay`로 올리지 않는다 (설계 §6)

완료 판정 (플레이 검증 완료 — `CoplayScripts/TestM3M4Behavior.cs`):

- ✅ 성이 다쳤을 때(50/100) 스킬 → `Activate=True`, 체력 50→100, **B 연출 0→1개 스폰**
- ✅ **연구 자동 회복 경로에는 연출이 안 뜬다** — `Castle.Repair`를 직접 불러 검증:
  체력 50→100으로 회복되지만 연출은 1→1 (늘지 않음)
- ✅ 성이 만피면 `Activate=False`, 연출 0→0
- ⬜ 밤에 실제로 눌러 A(화면 초록 배경) + B(성에 회복 파동)가 함께 뜨는지 눈으로 확인
- ⬜ 새끼용 생명 타워 힐 연출과 **상황으로** 구분된다 (같은 프리팹이므로 그림은 동일하다 —
  성 vs 타워 위치와 A 오버레이 동반 여부로 갈린다)
- ⬜ A와 B가 화면상 성 위에서 서로를 가리지 않는다

✅ **에셋 배선 완료** — `SK_Dragon_CastleHeal.asset`의 `TargetVfxPrefab`에
`FX_BD_LifeHealWave`(1.1초)를 채웠다 (`CoplayScripts/WireSkillTargetVfx.cs`).

커밋: `성벽 재생 스킬 연출 추가`

작업 로그:

- 2026-09-01 — 코드 완료. B는 신규 자산 없이 새끼용 프리팹 재사용으로 결정.
  `SkillSO.TargetVfxPrefab` + `Skill.PlayTargetVfx` 신설 (M5·M7이 재사용).
- 2026-09-01 — 에셋 배선 + 플레이 검증 완료. 회복 성공 시 B 스폰 확인,
  연구 자동 회복 경로에는 안 뜨는 것까지 확인.

---

## M5 — 전역 화염 A·B (불)

선행: M3 (M1의 N-cap 숫자 필요)

작업:

- **A**: Hovl `Side fire.prefab` 파생
- **B**: `Impact_BD_Fire.prefab` (설계 §7-2 — 새 경량화 자산을 만드는 것보다 싸다)
- 스폰 지점: `GlobalCurrentHealthDamageSkill.ApplyEffect`(`Skill.cs:417`)의 `TakeDamage` 직후,
  몬스터마다 `ProjectilePool.PlayForSeconds`. M1의 N-cap 적용
- 앵커는 몬스터 발밑 `MonsterMovement.GroundPlanePosition` (`SpriteRenderer.bounds` 금지 — 설계 §6)

### ⚠️ 함정

- ⚠️ **`DS_FireBurn.asset`의 `_activeVfxPrefab`은 비운 채로 둔다.** 불 각성 패시브
  (`DE_Fire_Unlock_R1_Passive.asset`)가 같은 SO를 참조해서, 채우면 **스킬을 안 써도 몬스터가 스폰될
  때마다 화면에 불이 상시로 뜬다.** 지속시간도 무한(`_durationSeconds: 0`)이다 (설계 §2-2).
  `DS_FireBurn_Persist.asset`도 마찬가지다
- ⚠️ B는 상태이상 파이프라인이 아니라 **스킬에서 직접** 스폰한다. "화상이 걸려 있다"가 아니라
  "지금 막 타격했다"는 신호다
- ⚠️ 20~30마리 동시가 이 스킬의 정상 상황이다. 원본 임팩트를 그대로 쓰면 시각 잡음이 화면을 덮는다

완료 판정:

- 불 각성 패시브만 켠 상태에서 스킬을 안 쓰면 화면에 화상 연출이 **안 뜬다** (회귀 확인)
- 몬스터 20마리 이상에서 발동해도 프레임이 유지되고 개별 타격이 읽힌다
- A가 뜨는 순간과 B가 뜨는 순간이 어긋나 보이지 않는다

커밋: `전역 화염 스킬 연출 추가`

작업 로그:

- (비어 있음)

---

## M6 — 즉시 재활성화 A·B (시간)

**"복구되며 빛이 차오른다"가 아니라 "기다림을 건너뛴다"다** (설계 §2-3).

선행: M3

작업:

- **A**: Hovl `Funnel yellow.prefab` 파생
- **B**: `Impact_BD_Time.prefab` 또는 `Muzzle_BD_Time.prefab` 베이스 (`Impact_BD_Time_2`도 있다)
- 스폰 지점: `Tower.RestoreAndReactivate(cause)`(`Tower.cs:496`)의
  `cause == ReactivationCause.SkillRepair` 분기. **`Tower`가 호스트다** — 이미 UniTask를 쓴다
- 기존 시각 상태와 **합성**한다: `IsBrokenPose`(`:133`) → `RefreshBrokenAnimation()`(`:136`)이
  부서진 포즈 → 정상 포즈 전환을 이미 하고 있고, 부활 게이지(`ReviveProgress` `:117`)는 스킬이
  중간에 끊는다. 새 애니메이션을 만들 필요가 없다

### ⚠️ 함정

- ⚠️ **`ReactivationCause`는 값이 3개다** (`Tower.cs:483`): `NightRevive`(부활 게이지 완주, `:416`) /
  `SkillRepair`(이 스킬, `:374`) / `MorningRestore`(아침 일괄 복구, `:273`).
  **셋 다 같은 메서드를 지나므로 분기 없이 스폰하면 자연 부활과 아침 복구에도 뜬다.**
- ⚠️ **`Building` 레이어는 뎁스 정렬이 있다.** `tower.DepthSortOrder + 1`을 상속한다
  (`TowerBuffOrbDisplay:199`와 같은 관용구). `Overlay`로 올리면 오히려 정렬을 잃는다 (설계 §6)
- ⚠️ **연구 미해금이면 아무 타워도 안 고쳐진다.** `CanUseCombatRepair`(`Tower.cs:441`)가 연구 해금
  질의라, `TryRestoreDuringCombat`(`:366`)이 전부 false를 돌려준다. **테스트 전에 해금부터 확인한다**
- ⚠️ 타워는 **파괴되지 않는다.** "파괴된 타워를 되살린다"는 프레이밍으로 연출을 만들지 않는다

완료 판정:

- 비활성 타워가 있을 때 스킬을 쓰면 그 타워들에만 연출이 뜨고 즉시 정상 포즈로 돌아온다
- **자연 부활(부활 게이지 완주)과 아침 일괄 복구에는 연출이 안 뜬다** — 둘 다 실제로 확인한다
- 연출이 타워보다 뒤로 밀리거나 다른 건물을 뚫고 앞으로 나오지 않는다
- 여러 타워가 동시에 켜져도 시각 잡음이 되지 않는다

커밋: `즉시 재활성화 스킬 연출 추가`

작업 로그:

- (비어 있음)

---

## M7 — 메테오 방벽 A·B + 방벽 생애주기 (암석)

**연출 대상이 3종류**다: 화면(A) / 피격 몬스터(B) / 방벽(B, 사건 3개).

선행: M3

작업:

- **A**: Hovl `Side highlights violet.prefab` 파생
- **B-몬스터**: `Impact_BD_Stone.prefab`. 반경 1.5라 대상이 한 자리 수 — N-cap 대상이 아니다
- **B-방벽 생성**: "땅이 솟아오르는" 마무리 플래시. 착지 폭발이 가라앉는 타이밍과 겹치지 않게
  **약 0.1~0.2초 늦게**. 정확한 값은 픽셀 측정으로 잡는다 (작업노트 3-9와 같은 종류)
- **B-방벽 소멸**: 밤 종료 시 사라지는 연출. `HandleNightEnd`(`StoneBarricade.cs:100`) 경로
- **B-방벽 피격·파괴**: 우선순위 낮음. **하한선은 "소멸만"이다** — 시간이 없으면 생략한다
- 호스트는 **`StoneBarricade`**(MonoBehaviour). `MeteorBarricadeSkill` 안에서는 지연 연출이 불가능하다
  (`Skill`은 MonoBehaviour가 아니다 — 설계 §1-1)
- 확대 베이스가 필요하면 `Assets/Imported/Prefabs/Projectile/Tower/Impact_Tower_Stone.prefab`

### ⚠️ 함정

- ⚠️ **`FX_Impact_StoneCrack`은 비참조 고립 에셋이다.** 런타임 배선본은 `Impact_Tower_Stone`이다.
  `Projectile_Tower_StoneMeteor`라는 이름은 존재하지 않는다 (설계 §7-3)
- ⚠️ **"몬스터만 맞고 방벽만 실패"하는 경로는 없다.** `MeteorBarricadeSkill.ApplyEffect`(`Skill.cs:585`)는
  설치 가능 여부를 데미지보다 **먼저** 검증하고 실패 시 `return false`로 데미지도 넣지 않는다.
  그 예외 처리를 만들지 않는다
- ⚠️ `StoneBarricade`에는 **VFX 훅이 하나도 없다.** 세 사건 모두 훅부터 만들어야 한다
- ⚠️ 방벽은 하루 2회 제한 스킬이라 **테스트 사이클이 짧다.** 낮/밤을 반복해서 확인해야 한다

완료 판정:

- 발동 → A(화면 보라 배경) → 착지 → B-몬스터 임팩트 → 살짝 늦게 방벽 마무리 플래시 → 방벽 등장,
  이 순서가 자연스럽게 읽힌다
- 방벽이 **예고 없이 나타나지 않고, 예고 없이 사라지지도 않는다**
- 설치 실패(경로 밖 지정 등) 시 오버레이도 임팩트도 안 뜬다
- 방벽이 밤 종료로 사라질 때 연출이 뜨고, 몬스터에게 부서질 때와 구분된다(피격 연출을 넣은 경우)

커밋: `메테오 방벽 스킬 연출 및 방벽 생애주기 연출 추가`

작업 로그:

- (비어 있음)

---

## M8 — 마감

선행: M4~M7

작업:

- **5종 연속 발동 회귀** — 앞 오버레이가 페이드 중일 때 다음 스킬을 눌러 겹침·잔상·미소멸을 본다
- 경계 상황
  - 속성 변경 직후 발동 (오버레이가 이전 속성으로 뜨지 않는지)
  - 밤 종료 순간 발동 (`SkillHudBinder.HideAll`이 타겟팅을 취소하는 시점)
  - 창이 열린 채 발동
  - 씬 전환·재시작 후 첫 발동 (정적 필드 자가등록이 리셋되는지 — `SubsystemRegistration`)
- 파티클 총량 측정, 임시 로그·디버그 옵션 제거
- 설계 문서 §8 열린 질문 중 해소된 것 정리, `[미정]`이 남아 있으면 이유를 적는다
- `CoplayScripts/`의 1회성 생성기 정리 여부 판단

완료 판정:

- 5종을 연달아 눌러도 오버레이가 쌓이거나 남지 않는다
- 콘솔에 신규 NullReference·누락 참조 없음
- 밤 웨이브 1회를 완주하며 프레임 하락이 관측되지 않는다
- 설계 문서와 이 문서의 진행 현황이 실제 상태와 일치한다

커밋: `어미용 스킬 이펙트 마감 및 회귀 확인`

작업 로그:

- (비어 있음)

---

## 뒤집힌 결정 (되돌리지 말 것)

옛 판의 문장을 보고 원복하는 사고를 막기 위한 절이다. 근거는 전부 설계 문서에 있다.

1. **A계층은 성 앵커가 아니라 화면 오버레이다.** 초판 §3("어미용은 성에 앵커")은 폐기됐다 —
   성에 붙은 월드 이펙트는 카메라가 성에서 멀면 화면에 안 보여서, §3 자신이 세운 요구를 만족하지 못했다.
   근거: 설계 §3-1
2. **"타워수리"가 아니라 "즉시 재활성화"다.** 타워는 파괴되지 않고 비활성화된다(CLAUDE.md).
   스트링테이블도 "즉시 재활성화"다. 근거: 설계 §2-3
3. **성벽 재생은 범위 안이다.** "4종"이라고 적힌 옛 문장은 폐기됐다. 기획·스트링테이블·에셋·코드 모두
   확정 상태였다. 근거: 설계 §2-5
4. **`ProjectilePool`에 오버로드를 추가할 필요가 없다.** 정렬·지연이 필요한 계층엔 이미 호스트가 있다
   (A=신규 컴포넌트 / 타워=`Tower` / 방벽=`StoneBarricade`). 추가하려거든 왜 필요한지 먼저 적을 것.
   근거: 설계 §1-1
5. **`Castle.Repair`와 `DS_FireBurn`에 연출을 걸지 않는다.** 둘 다 다른 기능(연구 회복 / 각성 패시브)과
   공유하는 경로라, 걸면 스킬을 안 써도 연출이 뜬다. 근거: 설계 §2-5, §2-2
6. **A계층 자산은 속성별로 개별 제작한다.** 하나의 파라미터화 프리팹을 색만 바꿔 재사용하는 안은
   검토했다가 반려됐다. 근거: 설계 §3-2
7. **오버레이 소팅은 `Highlight:-100`이다. `Overlay`로 되돌리지 말 것.** 레이어 이름이 `Overlay`라
   맞아 보이지만, 이 프로젝트 순서는 `… → Overlay → Projectile → Fog → Highlight`이고
   `FogCloudRenderer`의 구름이 **Fog**라 Overlay에 두면 구름에 덮인다. 근거: M2 「자산 검수 결과」
8. **성벽 재생 B는 새끼용 프리팹을 그대로 쓴다.** 설계 §2-5의 "파생본을 만들라"는 지시는
   M4에서 뒤집혔다. 성 1개에만 발동하고 A 오버레이가 함께 뜨므로 새끼용 타워 힐과 혼동되지 않는다.
   근거: M4 「뒤집힌 결정」
9. **`SkillUsed`를 조건 안으로 옮기지 말 것.** "눌렀다"(튜토리얼)와 "효과가 있었다"(연출)는 다른
   신호다. `SkillUsed`는 무조건 발화하고, 오버레이만 `Activate`의 반환값으로 거른다. 근거: M3
