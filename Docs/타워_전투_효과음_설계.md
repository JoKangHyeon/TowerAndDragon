# 타워 전투 효과음 설계 및 구현 계획

작성일: 2026-08-23
최종 갱신: 2026-08-24
상태: **설계 확정 · 구현 전**
관련 문서: [구현 현황](./구현현황.md) · [타워 투사체 연출 설계](./타워_투사체_연출_설계.md) · [타워 이펙트 작업 노트](./타워이펙트_작업노트.md)

이 문서는 다른 개발자나 AI 에이전트가 추가 판단 없이 타워 효과음 작업을 이어받을 수 있도록 만든 실행 명세다.
기획 내용은 `기획종합_v2.md`가 우선하며, 이 문서는 기획을 복제하지 않고 현재 코드에 효과음을 붙이는 방법만 다룬다.

> 프로젝트 규칙상 생성형 도구로 음악·SFX를 새로 만들지 않는다. 필요한 소리는 이미 팀 저장소에 있는
> `Assets/Imported/Gamemaster Audio - Pro Sound Collection/`에서 청취해 고른다. 적절한 클립이 없으면
> 임의로 생성하지 말고 팀에 에셋을 요청한다.

---

## 1. 확정 결정

다음 항목은 2026-08-24 논의에서 확정됐다. 작업자는 임의로 되돌리지 않는다.

1. 공격 또는 회복 투사체가 있는 타워는 **발동 시점의 Launch음**과 **도착 시점의 Resolve음** 두 가지를 사용한다.
   - 공격 타워의 Resolve는 명중/파열음이다.
   - 생명 타워의 Resolve는 파괴적인 충격음이 아니라 치유 적용음이다.
2. 시간·은신·영혼 타워는 투사체가 없으므로 Launch/Resolve음을 억지로 넣지 않는다.
   - 이번 범위에서는 오라가 비활성에서 활성으로 바뀔 때 `AuraOn`을 한 번만 재생한다.
   - 오라 지속 루프와 `AuraOff`는 이번 범위 밖이다.
3. 사운드 ID와 타워별 배선은 `TowerData`가 소유한다.
4. 신규 `SoundId`는 기존 enum과 `SoundCatalog.asset`의 **끝에만 추가**한다.
5. 이번 단계에서는 기존 2D SE 재생 방식을 유지한다. 위치 기반 감쇠는 실전 테스트 후 별도 결정한다.

### 용어

- **Launch**: 타워가 공격 또는 회복 투사체를 실제로 발사하는 순간. “머즐 효과음”과 같은 뜻으로 사용한다.
- **Resolve**: 투사체가 목적지에 도착해 명중 VFX와 공격/회복 효과를 적용하는 순간.
- **AuraOn**: 오라형 타워가 실제 가동 조건을 충족해 오라가 활성화되는 순간.

`Fire/Impact` 대신 `Launch/Resolve`를 쓰는 이유는 생명 타워의 치유도 같은 데이터 구조로 자연스럽게 표현하기 위해서다.

---

## 2. 범위와 타워 분류

### 2-1. 투사체·효과 적용형

| 타워 | 실제 데이터 | 이벤트 | 고유 사운드 쌍 | 방향 |
|---|---|---|---|---|
| 마나 | `TD_Arrow` | Launch + Resolve | `ArcaneBolt` 공유 | 짧고 밝은 에너지 방출 → 마법 파열 |
| 석궁 | `TD_CrossBow` | Launch + Resolve | `ArcaneBolt` 공유 | **짧은 화살이 나라아가는 느낌의 파열음** |
| 머스킷 | `TD_Musket` | Launch + Resolve | `Musket` | 묵직한 화약/압력 → 단단한 탄환 충격 |
| 대공 | `TD_Anti_Air` | Launch + Resolve | `AntiAir` | 길고 밝은 에너지 방출 -> 마법 파열 |
| 불 | `TD_FireTower` | Launch + Resolve | `Flame` | 화염 투사 → 폭발과 잔불 |
| 얼음 | `TD_IceTower` | Launch + Resolve | `Frost` | 결정성 냉기 → 얼음 파쇄 |
| 암석 | `TD_StoneTower` | Launch + Resolve | `Boulder` | 낮고 무거운 투척 → 암석 강타와 파편 |
| 생명 | `TD_LifeTower` | Launch + Resolve | `Life` | 따뜻한 치유 방출 → 부드러운 치유 적용 |

타워는 8종이지만 마나·석궁이 한 쌍을 공유하므로 **고유 Launch/Resolve 쌍은 7개, 고유 클립/ID는 14개**다.

### 2-2. 오라형

| 타워 | 실제 데이터 | 현재 동작 | 이번 이벤트 | 방향 |
|---|---|---|---|---|
| 시간 | `TD_TimeTower` | 공속·재활성화 속도 오라 | `AuraOn` | 짧은 틱 + 역재생 스웰 |
| 은신 | `TD_StealthTower` | 은신 오라 | `AuraOn` | 공기가 빨려드는 소리 + 얇은 쉬머 |
| 영혼 | `TD_SoulTower` | 보너스 인구 오라 | `AuraOn` | 낮은 공명 + 짧은 영혼성 숨결 |

오라형은 고유 ID 3개다. 따라서 이번 작업의 신규 `SoundId`와 고유 클립은 **총 17개**다.

### 2-3. 범위 밖

- 새끼용(`BD_*`), 몬스터, 용 스킬의 신규 효과음
  - 단, `TowerAttack`의 공용 발사음 제거로 새끼용이 조용해지는 회귀를 막기 위해 새끼용 데이터 5종의
    `_launchSound`에는 기존 `SoundId.TowerFire`를 마이그레이션 배선한다. 신규 클립이나 Resolve음은 추가하지 않는다.
- 타워 건설·피격·비활성화·부활 효과음
- 오라 해제음과 지속 루프
- 적 재질별 명중음 분기
- 위치 기반 3D/거리 감쇠
- 생성형 도구를 이용한 SFX 제작

---

## 3. 현재 코드 상태

### 3-1. 사운드 기반

현재 구조는 다음과 같다.

```text
SoundId enum
  ↓
SoundCatalog.asset
  └ SoundEntry { Clip, Volume, PitchRange, MinInterval }
  ↓
SoundManager.Play(SoundId)
  └ 2D AudioSource 풀 × 12, AudioMixer의 SE 그룹
```

이미 구현되어 있어 재사용할 기능:

- `SoundEntry.NextPitch`: 재생마다 피치를 무작위로 바꾼다.
- `SoundEntry.MinInterval`: 같은 ID가 너무 짧은 간격으로 겹치는 것을 막는다.
- `SoundManager.TakeSeSource()`: SE AudioSource 풀을 순환한다.
- 클립이 비어 있으면 경고 없이 무음 처리한다. 에셋 선정 전 상태도 안전하다.

### 3-2. 공격 타워 경로

```text
TowerAttack.Fire()
  ├ Launch음 재생 위치
  ├ 투사체 없음 → AttackSO.Execute()
  └ 투사체 있음 → ProjectilePool.Spawn()
                     → Projectile.Launch()
                     → 도착
                     → ApplyHit()
                     → ProjectileVisual.OnHit()
                     → Resolve음 재생 위치
                     → 풀 반납
```

현재 `TowerAttack.Fire()`는 일반 타워와 공격 모드 새끼용 모두에서
`SoundManager.Play(SoundId.TowerFire)`를 하드코딩해 호출한다.
`Projectile`은 발사한 타워의 사운드 정보를 보관하지 않는다.

### 3-3. 생명 타워 경로

생명 타워는 일반 적 공격 경로를 사용하지 않는다.

```text
LifeTower.CanAttackInCurrentMode == false
  ↓
TowerAllyHealer.Fire()
  ├ 현재 공용 TowerFire 재생
  ├ 아군 타워를 대상으로 Projectile 직접 Instantiate
  └ Projectile.Launch()
```

따라서 `TowerAttack`만 수정하면 생명 타워가 누락된다. `TowerAllyHealer`를 반드시 함께 수정한다.

### 3-4. 오라형 경로

`TD_TimeTower`, `TD_StealthTower`, `TD_SoulTower`는 `_attack`과 `_projectilePrefab`이 비어 있다.
오라 활성 여부는 `TowerAuraSystem.TryGetActiveAura()`가 다음 조건을 실시간으로 판정한다.

- 타워가 부활 중이 아님
- 마비 상태가 아님
- 유효한 `TowerAuraDataSO`가 있음
- 일반 타워는 `ITowerStaffing.CanOperate == true`
- 계산된 오라 반경이 0보다 큼

현재 오라 활성/비활성 전환 이벤트는 없다. `AuraOn`을 구현하려면 상태 전환을 감시하는 작은 컴포넌트가 필요하다.

### 3-5. 직렬화 주의사항

- 현재 `SoundId`는 0~13이며 `SoundCatalog.asset`이 정수로 직렬화한다.
- enum 중간에 새 값을 끼우면 기존 모든 사운드 배선이 밀린다.
- enum 기본값 0은 `UiButtonClick`이다.
- 새 `TowerData` 필드를 `SoundId`로 바로 선언하면 미배선 타워가 UI 클릭음을 낼 수 있다.

---

## 4. 데이터 설계

### 4-1. `OptionalSoundId`

신규 파일: `Assets/Scripts/Sound/OptionalSoundId.cs`

```csharp
[System.Serializable]
public struct OptionalSoundId
{
    [Tooltip("끄면 이 사운드는 재생되지 않는다. 값을 채우지 않은 기존 에셋의 기본 상태다.")]
    [SerializeField] private bool _isEnabled;

    [SerializeField] private SoundId _id;

    public SoundId? Value => _isEnabled ? _id : null;
}
```

기본값이 무음이므로 미배선 데이터가 엉뚱한 UI 클릭음을 내지 않는다. 다만 현재 공용 `TowerFire`를 내는
새끼용까지 자동으로 무음이 되므로, 새끼용 데이터 5종에는 기존 `TowerFire`를 명시적으로 배선해 동작을 보존한다.

### 4-2. `TowerData` 필드

수정 파일: `Assets/Scripts/Buildings/Tower/TowerData.cs`

```csharp
[Header("Sound")]
[Tooltip("공격 또는 회복 투사체를 발동하는 순간 재생할 효과음.")]
[SerializeField] private OptionalSoundId _launchSound;

[Tooltip("투사체가 목적지에 도착해 공격 또는 회복 효과를 적용할 때 재생할 효과음.")]
[SerializeField] private OptionalSoundId _resolveSound;

[Tooltip("오라가 비활성에서 활성으로 바뀔 때 한 번 재생할 효과음.")]
[SerializeField] private OptionalSoundId _auraOnSound;

public SoundId? LaunchSound => _launchSound.Value;
public SoundId? ResolveSound => _resolveSound.Value;
public SoundId? AuraOnSound => _auraOnSound.Value;
```

사운드를 `AttackSO`에 두지 않는다. `AttackSO`는 일반 타워와 새끼용이 공유할 수 있어 한쪽 배선이 다른 쪽으로 전파되기 때문이다.
오라음도 `TowerAuraDataSO`가 아니라 `TowerData`에 둔다. 오라 수치가 아니라 소리를 내는 타워의 정체성에 속하는 데이터다.

---

## 5. `SoundId` 추가 목록

수정 파일: `Assets/Scripts/Sound/SoundId.cs`

기존 0~13은 수정·삭제·재정렬하지 않고 아래 값을 끝에 추가한다.

| 값 | ID | 배선 대상 |
|---:|---|---|
| 14 | `TowerArcaneBoltLaunch` | 마나·석궁 Launch 공유 |
| 15 | `TowerArcaneBoltResolve` | 마나·석궁 Resolve 공유 |
| 16 | `TowerMusketLaunch` | 머스킷 Launch |
| 17 | `TowerMusketResolve` | 머스킷 Resolve |
| 18 | `TowerAntiAirLaunch` | 대공 Launch |
| 19 | `TowerAntiAirResolve` | 대공 Resolve |
| 20 | `TowerFlameLaunch` | 불 Launch |
| 21 | `TowerFlameResolve` | 불 Resolve |
| 22 | `TowerFrostLaunch` | 얼음 Launch |
| 23 | `TowerFrostResolve` | 얼음 Resolve |
| 24 | `TowerBoulderLaunch` | 암석 Launch |
| 25 | `TowerBoulderResolve` | 암석 Resolve |
| 26 | `TowerLifeLaunch` | 생명 회복 투사체 Launch |
| 27 | `TowerLifeResolve` | 생명 치유 적용 Resolve |
| 28 | `TowerTimeAuraOn` | 시간 오라 활성화 |
| 29 | `TowerStealthAuraOn` | 은신 오라 활성화 |
| 30 | `TowerSoulAuraOn` | 영혼 오라 활성화 |

기존 `SoundId.TowerFire`는 삭제하지 않는다. 삭제하면 뒤쪽 기존 ID의 직렬화 값이 밀린다. 이번 타워 배선에서만 사용을 중단한다.

---

## 6. 런타임 구현 명세

### 6-1. `TowerAttack`

수정 파일: `Assets/Scripts/Buildings/Tower/TowerAttack.cs`

`Fire()`의 공용 호출을 데이터 기반 호출로 교체한다.

```csharp
if (_towerData.LaunchSound is SoundId launchSound)
{
    SoundManager.Play(launchSound);
}
```

투사체를 발사할 때 Resolve음을 전달한다.

```csharp
projectile.Launch(
    target,
    Attack,
    in context,
    _towerData.ProjectileSpeed,
    _towerData.ResolveSound);
```

### 6-2. `TowerAllyHealer`

수정 파일: `Assets/Scripts/Buildings/Tower/TowerAllyHealer.cs`

- `Fire()`의 `SoundId.TowerFire` 하드코딩을 `Data.LaunchSound`로 교체한다.
- `Projectile.Launch()`에 `Data.ResolveSound`를 전달한다.
- 생명 타워의 Launch/Resolve가 일반 공격 타워와 같은 시간 규칙을 따르게 한다.

생명 타워는 풀을 사용하지 않고 투사체를 직접 `Instantiate`하지만, Resolve음의 보관과 재생은 `Projectile`이 담당하므로 별도 명중 로직을 만들지 않는다.

### 6-3. `Projectile`

수정 파일: `Assets/Scripts/Combat/Projectile.cs`

```csharp
private SoundId? _resolveSound;

public void Launch(
    IAttackTarget target,
    AttackSO attack,
    in AttackContext context,
    float speed,
    SoundId? resolveSound = null)
```

구현 규칙:

1. `Launch()` 첫 줄의 `ResetState()`는 유지한다.
2. 타겟·공격·속도 검증 실패 경로를 모두 지난 뒤 `_resolveSound = resolveSound`로 저장한다.
3. `ResetState()`에서 `_resolveSound = null`로 초기화한다.
4. 도착 시 순서는 다음과 같이 유지한다.

```text
ApplyHit()
→ ProjectileVisual.OnHit()
→ Resolve음 재생
→ Release()
```

명중 VFX와 Resolve음은 같은 프레임에 발생해야 한다. 투사체가 날아가는 동안 원래 타겟이 죽더라도 현재 VFX가 목적지에서 터지는 규칙과 맞춰 Resolve음도 재생한다.

`resolveSound`는 기본 인자 `null`로 둔다. 기존 `MonsterAttack` 호출부를 수정하지 않아도 몬스터 투사체는 무음으로 유지된다.

### 6-4. `TowerAuraSoundEmitter`

신규 파일: `Assets/Scripts/Sound/TowerAuraSoundEmitter.cs`

이 컴포넌트는 시간·은신·영혼 타워 프리팹에만 붙인다.

책임:

- `Awake()`에서 같은 오브젝트의 `Tower`를 캐시한다.
- `LateUpdate()`에서 `TowerAuraSystem.TryGetActiveAura(tower, out _, out _)`로 현재 활성 여부를 구한다.
- 첫 관측 시 활성 상태라면 `TowerData.AuraOnSound`를 한 번 재생한다.
- 이후 `false → true` 전환에서만 다시 재생한다.
- `true → false`는 상태만 저장하고 소리는 내지 않는다.
- `Update()`가 아니라 `LateUpdate()`를 사용해 `Tower.Start()/Setup()` 이후 데이터를 관측한다.

필요 상태 예시:

```csharp
private Tower _tower;
private bool _hasObservedState;
private bool _wasAuraActive;
```

재생 조건은 타워가 존재한다는 사실이 아니라 **실제로 오라가 활성화됐는지**다. 따라서 인구 0, 마비, 부활 중 상태에서는 재생하지 않고, 인구가 들어오거나 재활성화되어 오라가 다시 켜질 때 재생한다.

프리팹 배선 대상:

- `Assets/Data/TowerData/TowerPrefab/TP_TimeTower.prefab`
- `Assets/Data/TowerData/TowerPrefab/TP_StealthTower.prefab`
- `Assets/Data/TowerData/TowerPrefab/TP_SoulTower.prefab`

Unity 에디터의 프리팹·컴포넌트 조작은 프로젝트 규칙대로 Coplay MCP를 사용한다.

---

## 7. 클립 선정 지침

### 7-1. 현재 에셋 상태

사운드를 새로 생성할 필요는 없다. Pro Sound Collection에 후보 파일이 실제로 존재하지만, 아직 최종 청취·선정은 하지 않았다.
파일명만 보고 확정하지 말고 Unity Project 창 또는 외부 미리듣기로 반드시 들어본다.

각 ID마다 다음 절차를 따른다.

1. 후보 2~3개를 고른다.
2. 단독 청취가 아니라 실제 투사체 VFX와 함께 재생한다.
3. 가장 어울리는 1개를 최종 선택한다.
4. 반복 피로도는 여러 클립이 아니라 `PitchRange`로 우선 완화한다.
5. 원본이 너무 길거나 무음 구간이 있으면 팀이 허용한 편집 도구로 복사본을 트리밍한다.
6. 적합한 후보가 없으면 생성형 도구를 쓰지 말고 팀에 요청한다.

같은 사운드 쌍 안에서도 Launch와 Resolve는 서로 다른 클립을 사용한다. 마나·석궁이 공유한다는 뜻은
두 타워가 같은 쌍을 쓴다는 뜻이지, Launch와 Resolve를 한 클립으로 통일한다는 뜻이 아니다.

### 7-2. 후보와 검색 시작점

아래는 확정 파일이 아니라 청취 시작점이다.

| ID 계열 | 폴더 | 우선 후보/검색어 |
|---|---|---|
| `ArcaneBolt` Launch | `Magic_Spells/` | `energy_blast_small_*`, `whoosh_magic_spell_*` |
| `ArcaneBolt` Resolve | `Magic_Spells/` | `magic_pop_open_*`, 짧은 `energy_blast_small_*` 중 Launch와 다른 클립 |
| `Musket` Launch | `Guns_Weapons/Guns/` | `gun_rifle_shot_*` |
| `Musket` Resolve | `Impacts_Smashable/`, `Metal/` | 짧고 단단한 bullet/metal impact 계열 |
| `AntiAir` Launch | `Magic_Spells/` | `electric_lightning_blast_*` |
| `AntiAir` Resolve | `Magic_Spells/` | `electric_surge_blast_*`, Launch와 다른 짧은 전기 파열음 |
| `Flame` Launch | `Magic_Spells/` | `fireball_blast_projectile_spell_*` |
| `Flame` Resolve | `Magic_Spells/` | `fireball_impact_burn_*`, `fireball_impact_sizzle_burn*` |
| `Frost` Launch | `Magic_Spells/` | `ice_blast_projectile_spell_*` |
| `Frost` Resolve | `Magic_Spells/` | `ice_spell_impact_hit_shard_*`, `ice_spell_impact_shatter_*` |
| `Boulder` Launch | `Impacts_Smashable/` | `rock_blast_impact_projectile_*` |
| `Boulder` Resolve | `Impacts_Smashable/` | `rock_impact_heavy_slam_*` |
| `Life` Launch/Resolve | `Magic_Spells/` | `healing_magic_spell_*`, `magic_shine_light_spell_*`, `chimes_magic_bell_ding_*` |
| `TimeAuraOn` | `Magic_Spells/` | `time_warp_reverse_spell_cast_*`, `time_warp_reverse_high_*` |
| `StealthAuraOn` | `Magic_Spells/` | `vanish_spell_flash_potion_*`, `dark_wind_suck_conjure_*` |
| `SoulAuraOn` | `Magic_Spells/`, `Voice/Ghost/` | `dark_magic_conjure_blast_*`, 짧은 `ghost_witch_voice_hiss_*` |

### 7-3. 마나·석궁 공용음 제한

마나·석궁 공용음에는 현실적인 석궁 시위음이나 순수한 마법 차임 한쪽을 과도하게 강조하지 않는다.
둘 모두에 붙여도 어색하지 않은 **짧은 에너지 방출 + 공기 절단** 계열을 우선한다.

두 데이터는 반드시 다음 항목까지 동일하게 배선한다.

- 같은 `Launch` ID
- 같은 `Resolve` ID
- 같은 `SoundCatalog` 항목이므로 클립·볼륨·피치·최소 간격도 자동 공유

별도의 마나/석궁 ID를 만들지 않는다.

---

## 8. 초기 믹싱 값

아래 값은 첫 플레이 테스트를 위한 시작점이다. 청취 결과에 따라 `SoundCatalog.asset`만 조정하고 코드에 볼륨·피치 숫자를 넣지 않는다.

| ID 계열 | Volume | PitchRange | MinInterval | 비고 |
|---|---:|---|---:|---|
| `ArcaneBoltLaunch` | 0.35 | 0.94~1.06 | 0.20 | 마나·석궁 공동 쿨다운 |
| `ArcaneBoltResolve` | 0.42 | 0.94~1.06 | 0.20 | Launch보다 약간 강조 |
| `MusketLaunch` | 0.45 | 0.96~1.04 | 0.30 | 저중역 과다 주의 |
| `MusketResolve` | 0.48 | 0.96~1.04 | 0.30 | 총성보다 짧게 |
| `AntiAirLaunch` | 0.40 | 0.95~1.05 | 0.30 | 고주파 피로도 확인 |
| `AntiAirResolve` | 0.48 | 0.95~1.05 | 0.30 | 짧은 스파크 |
| `FlameLaunch` | 0.40 | 0.94~1.06 | 0.20 | 지속 노이즈가 길지 않게 |
| `FlameResolve` | 0.55 | 0.94~1.06 | 0.20 | 파열 강조 |
| `FrostLaunch` | 0.40 | 0.94~1.06 | 0.20 | 결정성 고역 |
| `FrostResolve` | 0.55 | 0.94~1.06 | 0.20 | 유리처럼 날카롭지 않게 |
| `BoulderLaunch` | 0.45 | 0.96~1.04 | 0.35 | 저빈도·중량감 |
| `BoulderResolve` | 0.70 | 0.96~1.04 | 0.35 | 가장 큰 명중 강조 |
| `LifeLaunch` | 0.35 | 0.97~1.03 | 0.25 | 전투음보다 부드럽게 |
| `LifeResolve` | 0.42 | 0.97~1.03 | 0.25 | 짧은 치유 차임 |
| `TimeAuraOn` | 0.45 | 0.98~1.02 | 0.50 | 상태 전환 1회 |
| `StealthAuraOn` | 0.40 | 0.98~1.02 | 0.50 | 상태 전환 1회 |
| `SoulAuraOn` | 0.45 | 0.98~1.02 | 0.50 | 음성처럼 길지 않게 |

마나와 석궁이 같은 ID를 공유하므로 같은 프레임에 함께 발사하면 `MinInterval`도 공유한다. 이는 두 타워를 같은 음향 계열로 묶고 중첩 폭증을 막으려는 의도다.

### 클립 길이

Resolve음은 대응 VFX보다 오래 남지 않게 한다.

| 타워 | 목표 길이 |
|---|---:|
| 마나·석궁 | 약 0.5초 이하 |
| 머스킷·대공 | 약 0.5초 이하 |
| 불 | 약 0.8초 이하 |
| 얼음 | 약 1.2초 이하 |
| 암석 | 약 0.9초 이하 |
| 생명 | 약 0.8초 이하 |
| AuraOn 3종 | 약 1.0초 이하 |

### AudioSource 풀

현재 `_sePoolSize`는 12다. Launch와 Resolve가 동시에 늘어나므로 다음 씬의 `SoundManager`를 24로 올린다.

- `Assets/Scenes/SampleScene.unity` — 필수
- `Assets/Scenes/Tutorial.unity` — 필수
- 타워 전투 작업용 씬 — 권장
- 전투가 없는 `StartScene` — 변경 불필요

씬 오브젝트 수정과 플레이 검증은 Coplay MCP로 수행한다.

---

## 9. 마일스톤

### M0 — 범위·데이터 계약 반영

상태: ⬜ 대기

작업:

- 이 문서의 ID 표와 실제 `SoundId` 기존 번호 재확인
- `OptionalSoundId` 추가
- `TowerData`에 Launch/Resolve/AuraOn 필드 추가
- `SoundId` 17개를 14~30에 append-only로 추가
- 컴파일 확인

완료 조건:

- 기존 0~13 값과 `SoundCatalog.asset` 기존 `_id`가 바뀌지 않음
- 미배선 데이터의 기본값이 무음
- 컴파일 에러 0

### M1 — 투사체 Launch/Resolve 런타임 연결

상태: ⬜ 대기

작업:

- `TowerAttack`의 공용 `TowerFire` 제거 및 데이터 기반 Launch 재생
- `Projectile.Launch()`에 nullable Resolve음 전달
- 도착 시 VFX와 같은 프레임에 Resolve음 재생
- 풀 상태 초기화
- `TowerAllyHealer`의 생명 타워 경로도 동일하게 연결

완료 조건:

- 공격 타워와 생명 타워 모두 Launch/Resolve가 각각 한 번씩 발생
- 몬스터와 새끼용에 신규 타워음이 새지 않음
- 재사용 투사체가 이전 Resolve음을 기억하지 않음
- 컴파일 에러 0

### M2 — 오라 AuraOn 런타임 연결

상태: ⬜ 대기

작업:

- `TowerAuraSoundEmitter` 추가
- 시간·은신·영혼 프리팹에 컴포넌트 배선
- 최초 활성 및 `false → true` 전환 재생 확인
- 비활성 상태와 `true → false`에서는 무음 확인

완료 조건:

- 타워 배치 직후 실제 가동 조건을 만족하면 1회 재생
- 인구 0인 시간·은신 타워는 재생하지 않음
- 인구 투입, 마비 해제, 부활 등으로 다시 활성화될 때 1회 재생
- 프레임마다 반복 재생되지 않음

### M3 — 클립 청취·선정

상태: ⬜ 대기

작업:

- §7 후보를 각 ID별 2~3개 청취
- VFX와 함께 A/B 비교
- 최종 17개 선정
- 필요하면 비파괴 복사본 트리밍
- 적합한 후보가 없으면 팀에 요청

완료 조건:

- 마나·석궁 공용음이 양쪽 모두에서 어색하지 않음
- 머스킷과 대공이 귀로 구분됨
- 불·얼음·암석의 재질감이 분명함
- 생명음이 공격 명중음처럼 들리지 않음
- 오라음이 1초 내외로 짧고 반복 피로가 적음

### M4 — 카탈로그·타워 데이터·씬 배선

상태: ⬜ 대기

작업:

- `SoundCatalog.asset` 끝에 17개 `SoundEntry` 추가
- §8 초기 믹싱 값 입력
- 타워 데이터 11종 배선
- 새끼용 데이터 5종의 Launch에 기존 `TowerFire` 배선(회귀 방지용, Resolve/AuraOn은 없음)
- 마나·석궁의 두 필드가 같은 ID인지 재확인
- `SampleScene`, `Tutorial`의 `_sePoolSize`를 24로 변경

완료 조건:

- 기존 SoundEntry의 `_id`와 클립 참조가 바뀌지 않음
- 모든 신규 ID가 카탈로그에 정확히 1회 존재
- 11개 타워 데이터에 필요한 값이 누락되지 않음
- `Assets/Imported`에 의도하지 않은 변경이 없음

### M5 — 단일 타워 기능 검증

상태: ⬜ 대기

Coplay MCP 플레이 모드에서 각 타워를 한 종류씩 검증한다.

완료 조건:

- §10의 단일 타워 검증표 전부 통과
- 컴파일 에러 0
- 플레이 모드 진입 가능
- 콘솔에 신규 NullReference/누락 참조 오류 없음

### M6 — 실전 웨이브 믹싱·마감

상태: ⬜ 대기

작업:

- 타워 10대 이상으로 밤 웨이브 완주
- 1배속·2배속 비교
- 볼륨·피치·MinInterval 조정
- SE 0 설정 검증
- 최종 선택 클립과 수치를 이 문서에 기록

완료 조건:

- 소리가 중간에 자주 끊기지 않음
- 동일 타워 다수가 동시에 발사해도 볼륨이 폭증하지 않음
- BGM·몬스터 핵심음·UI음을 과도하게 가리지 않음
- 2배속에서도 소음 덩어리가 되지 않음

---

## 10. 플레이 검증표

### 10-1. 투사체·효과 적용형

- [ ] 마나 한 대: Launch와 Resolve가 각각 한 번 발생한다.
- [ ] 석궁 한 대: 마나와 **완전히 같은 음색**이 난다.
- [ ] 마나와 석궁 동시 배치: 공격 리듬만 다르고 클립은 동일하다.
- [ ] 머스킷: 마나·석궁보다 묵직하고 대공과 구분된다.
- [ ] 대공: 공중 적이 있는 웨이브에서 Launch/Resolve가 발생한다.
- [ ] 불: 화염 Launch 뒤 VFX가 터지는 순간 화염 Resolve가 난다.
- [ ] 얼음: 얼음 VFX와 Resolve가 같은 프레임에 발생한다.
- [ ] 암석: 가장 무겁고 큰 Resolve가 나며 클립이 VFX보다 길게 남지 않는다.
- [ ] 생명: 피해 입은 아군 타워를 회복할 때 Launch/Resolve가 난다.
- [ ] 생명: 회복 대상이 없을 때 반복 소리가 나지 않는다.
- [ ] 타겟이 비행 중 죽어도 기존 명중 VFX와 Resolve음이 함께 발생한다.

### 10-2. 오라형

- [ ] 시간: 인구 0에서는 무음이고 인구 투입으로 오라가 켜질 때 1회 재생한다.
- [ ] 은신: 시간 타워와 같은 상태 규칙을 따르되 다른 클립을 쓴다.
- [ ] 영혼: 항상 가동 조건을 만족하면 배치 후 1회 재생한다.
- [ ] 마비/부활 등으로 비활성화될 때는 소리가 나지 않는다.
- [ ] 다시 활성화될 때 AuraOn이 정확히 1회 난다.
- [ ] 활성 상태로 유지되는 동안 프레임마다 반복되지 않는다.

### 10-3. 회귀·믹싱

- [ ] 공격 모드 새끼용은 기존 공용 `TowerFire`를 유지하고 신규 타워음은 내지 않는다.
- [ ] 몬스터 투사체는 신규 타워 Resolve음을 내지 않는다.
- [ ] 시간 타워는 공격하지 않으며 Launch/Resolve도 내지 않는다.
- [ ] 타워 10대 이상에서 SE가 자주 뚝 끊기지 않는다.
- [ ] SE 볼륨 0에서 모든 신규 소리가 사라지고 BGM은 남는다.
- [ ] 2배속에서도 과도한 소음이 되지 않는다.
- [ ] 설정·UI·건설 등 기존 0~13 사운드가 이전과 동일하다.

---

## 11. 에셋 배선표

| TowerData | Launch | Resolve | AuraOn |
|---|---|---|---|
| `TD_Arrow` | `TowerArcaneBoltLaunch` | `TowerArcaneBoltResolve` | 없음 |
| `TD_CrossBow` | `TowerArcaneBoltLaunch` | `TowerArcaneBoltResolve` | 없음 |
| `TD_Musket` | `TowerMusketLaunch` | `TowerMusketResolve` | 없음 |
| `TD_Anti_Air` | `TowerAntiAirLaunch` | `TowerAntiAirResolve` | 없음 |
| `TD_FireTower` | `TowerFlameLaunch` | `TowerFlameResolve` | 없음 |
| `TD_IceTower` | `TowerFrostLaunch` | `TowerFrostResolve` | 없음 |
| `TD_StoneTower` | `TowerBoulderLaunch` | `TowerBoulderResolve` | 없음 |
| `TD_LifeTower` | `TowerLifeLaunch` | `TowerLifeResolve` | 없음 |
| `TD_TimeTower` | 없음 | 없음 | `TowerTimeAuraOn` |
| `TD_StealthTower` | 없음 | 없음 | `TowerStealthAuraOn` |
| `TD_SoulTower` | 없음 | 없음 | `TowerSoulAuraOn` |

Coplay 또는 에디터 스크립트로 일괄 배선한 뒤 인스펙터에서 표와 한 줄씩 대조한다.

새끼용 5종은 이번 타워 분류표에는 포함하지 않지만 회귀 방지 마이그레이션이 필요하다.

| BabyDragonData | Launch | Resolve | AuraOn |
|---|---|---|---|
| `BD_Fire` | 기존 `TowerFire` | 없음 | 없음 |
| `BD_Ice` | 기존 `TowerFire` | 없음 | 없음 |
| `BD_Time` | 기존 `TowerFire` | 없음 | 없음 |
| `BD_Stone` | 기존 `TowerFire` | 없음 | 없음 |
| `BD_Life` | 기존 `TowerFire` | 없음 | 없음 |

---

## 12. 변경 파일 예상 목록

### 코드

- 신규 `Assets/Scripts/Sound/OptionalSoundId.cs`
- 신규 `Assets/Scripts/Sound/TowerAuraSoundEmitter.cs`
- 수정 `Assets/Scripts/Sound/SoundId.cs`
- 수정 `Assets/Scripts/Buildings/Tower/TowerData.cs`
- 수정 `Assets/Scripts/Buildings/Tower/TowerAttack.cs`
- 수정 `Assets/Scripts/Buildings/Tower/TowerAllyHealer.cs`
- 수정 `Assets/Scripts/Combat/Projectile.cs`

### 데이터·프리팹·씬

- 수정 `Assets/Data/SoundCatalog.asset`
- 수정 `Assets/Data/TowerData/TD_*.asset` 대상 11개
- 수정 `Assets/Data/BabyDragon/*/BD_*.asset` 대상 5개(기존 `TowerFire` 마이그레이션만)
- 수정 오라 타워 프리팹 3개
- 수정 `Assets/Scenes/SampleScene.unity`
- 수정 `Assets/Scenes/Tutorial.unity`

### 저장소 경계

신규 코드는 메인 저장소에 들어간다. 기존 Pro Sound Collection 파일을 참조만 하면 `Assets/Imported` 저장소에는 변경이 없어야 한다.
원본을 트리밍한 복사본이 꼭 필요하면 저장 위치와 공유 저장소를 먼저 팀과 합의한다. 임의로 원본 WAV를 덮어쓰지 않는다.

---

## 13. 인계 체크리스트

새 에이전트는 작업을 시작할 때 다음 순서를 지킨다.

1. `AGENTS.md`, `Docs/구현현황.md`, 이 문서를 읽는다.
2. `git status --short`로 기존 사용자 변경을 확인하고 보존한다.
3. `SoundId.cs`와 `SoundCatalog.asset`의 현재 마지막 ID가 13인지 확인한다.
4. M0부터 순서대로 진행하며 한 번에 하나의 마일스톤만 완료 처리한다.
5. Unity 에디터 작업·컴파일 확인·플레이 테스트는 Coplay MCP를 사용한다.
6. 생성형 SFX 기능은 사용하지 않는다.
7. 각 M 완료 후 이 문서의 상태와 실제 결과를 갱신한다.
8. 코드 본문에 볼륨·피치·간격 리터럴을 넣지 않는다. 해당 값은 `SoundCatalog.asset`에 둔다.
9. 커밋 전 컴파일 에러 0과 플레이 모드 진입 가능 여부를 확인한다.
10. 문서와 커밋 메시지는 한국어로 작성한다.

권장 커밋 분리:

1. `타워 전투 효과음 데이터 구조 추가`
2. `타워 투사체 발사 및 명중 효과음 연동`
3. `오라 타워 활성 효과음 연동`
4. `타워별 효과음 에셋 배선 및 믹싱 적용`
5. `타워 전투 효과음 플레이 검증 및 수치 조정`

---

## 14. 열린 문제

### 화면 밖 소리

현재 SE는 모두 2D라 지도 반대편 소리도 같은 크기로 들린다. 이번에는 `MinInterval`과 볼륨으로 먼저 밀도를 잡는다.
실전 웨이브에서 문제가 확인된 경우에만 후속 작업으로 `SoundManager.PlayAt(SoundId, Vector3)`와 카메라 거리 컷오프를 설계한다.

### 오라 해제음·루프

현재는 `AuraOn`만 구현한다. 타워마다 루프를 틀면 여러 채가 있을 때 지속적인 소음이 되므로 보류한다.
필요성이 확인되면 “카메라 근처 또는 선택된 타워만 루프” 같은 제한과 함께 별도 설계한다.

### 클립 최종 확정

§7의 파일명은 후보일 뿐이다. 최종 클립명, 길이, 볼륨, 피치, MinInterval은 M3~M6 결과로 이 문서에 기록해야 한다.
