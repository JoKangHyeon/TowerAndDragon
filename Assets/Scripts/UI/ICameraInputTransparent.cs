/// <summary>
/// 이 UI가 덮고 있어도 카메라 조작은 그대로 통과시킨다.
///
/// 카메라는 포인터가 UI 위에 있으면 휠 줌을 접는다 - 스크롤 패널(연구 트리, 점령 비용 목록) 위에서
/// 휠을 굴렸을 때 그 패널 대신 화면이 확대되면 안 되기 때문이다. 그런데 그 판정은
/// <c>EventSystem.IsPointerOverGameObject</c> 하나뿐이라, <b>스크롤할 것이 없는 전면 차단막</b>도
/// 똑같이 UI로 친다. 안내 딤이 그렇다 - 화면을 덮는 동안 휠 줌이 통째로 죽는다.
///
/// 그 막을 구현체로 표시해 카메라만 예외로 본다. 클릭 차단은 그대로다 -
/// 딤의 일은 엉뚱한 버튼이 눌리지 않게 하는 것이지 화면을 못 움직이게 하는 것이 아니다.
/// </summary>
public interface ICameraInputTransparent
{
}
