/// <summary>
/// 퀘스트 설명에 <b>지금 값</b>을 덧붙일지, 붙인다면 무엇을 붙일지.
///
/// 설명문에 숫자를 박아 두면 그 숫자는 쓰는 순간에만 참이다 - 실제로 "인구 40명이니 하루 40이 나가고
/// 재고 250으로는 엿새"라고 적어 두었더니, 2일차에 열어 보거나 인구가 바뀌면 화면과 말이 갈렸다.
/// 그래서 시점에 따라 달라지는 숫자는 문안에서 빼고 표시할 때 계산해 덧붙인다.
///
/// 정수로 직렬화되므로 <b>새 값은 반드시 끝에 추가한다</b>.
/// </summary>
public enum GuideQuestBodyArgument
{
    // 덧붙일 것이 없다. 대부분의 퀘스트가 이쪽이다 - 건설 비용처럼 변하지 않는 숫자는 문안에 적어도 된다.
    None = 0,

    // 지금 식량 보유량 · 하루 증감 · 바닥날 때까지 남은 날.
    FoodOutlook,
}
